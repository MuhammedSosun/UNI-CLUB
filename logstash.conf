input {
  tcp {
    port => 5000
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://uniElasticSearch:9200"]
    index => "uniclub-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
